# SEO Stage 1 — Quick Win Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Внедрить технический SEO-фундамент (sitemap, robots, schema.org, OG, canonical, локальные title/description, alt-теги портфолио) и подготовить runbook'и для Я.Бизнес и Я.Вебмастера, чтобы Яндекс начал целенаправленно индексировать `agentpermanent.ru` и в выдаче появились локальные сниппеты со звёздами 5.0/187 отзывов.

**Architecture:**
- Сайт — статический HTML на Cloudflare Pages, без сборщика. Все SEO-операции делаем через **набор небольших Python-скриптов** в `tools/seo/`, идемпотентных (повторный запуск не дублирует тегов).
- Каждый скрипт отвечает за одну задачу: `generate_sitemap.py`, `inject_canonical_og.py`, `inject_schema.py`, `update_titles.py`, `fix_alt_tags.py`. Это легко поддерживать, легко тестировать (внутренние ассерты), легко запускать выборочно.
- **Smoke-тест** (`tools/seo/smoke_test.sh`) пробегает по prod-страницам и проверяет наличие всех мета-блоков. Запускается перед каждым деплоем.
- Я.Бизнес и Я.Вебмастер — ручные действия владельца (Игорь). Документируем их в `docs/superpowers/runbooks/`.

**Tech Stack:** Python 3.9 (stdlib, без зависимостей), bash + curl + grep для smoke-тестов, sips (macOS native) для обрезки картинок, существующий `wrangler` для деплоя.

---

## File Structure

**Создаём:**
- `tools/seo/generate_sitemap.py` — генератор `sitemap.xml` из списка production-страниц.
- `tools/seo/inject_canonical_og.py` — добавляет `<link rel="canonical">`, `og:*`, `twitter:card` теги в `<head>` каждой production-страницы.
- `tools/seo/inject_schema.py` — добавляет JSON-LD блоки `Organization`, `BeautySalon`, `Course`, `BreadcrumbList`, `AggregateRating` в `<head>` по соответствию страница→тип.
- `tools/seo/update_titles.py` — переписывает `<title>` и `<meta name="description">` под локальные ключи.
- `tools/seo/fix_alt_tags.py` — заполняет пустые `alt=""` в `portfolio/index.html` по шаблону на основе пути файла.
- `tools/seo/smoke_test.sh` — bash-проверка что все теги на месте (запуск перед деплоем).
- `tools/seo/run_all.sh` — последовательный запуск всех генераторов.
- `tools/seo/PAGES.py` — единый список production-страниц с метаданными (used by 4 скрипта).
- `sitemap.xml` — output генератора.
- `robots.txt` — статичный файл вместо CF-дефолтного.
- `og-image-studio.jpg` — превью 1200×630 для главной/студии/контактов.
- `og-image-academy.jpg` — превью 1200×630 для академии.
- `docs/superpowers/runbooks/yandex-business-setup.md` — пошаговая инструкция для Игоря по доводке карточки Я.Бизнес.
- `docs/superpowers/runbooks/yandex-webmaster-setup.md` — регистрация сайта в Я.Вебмастере.

**Модифицируем (все 9 production-страниц):**
- `index.html`, `studio/index.html`, `school/index.html`, `school/pm/index.html`, `school/brows/index.html`, `portfolio/index.html`, `about/index.html`, `contacts/index.html`, `privacy/index.html`.

В каждой добавляем: `<link rel="canonical">`, `og:*` теги, `twitter:card`, JSON-LD blocks. Все вставки идемпотентны и помечены HTML-комментариями (`<!-- SEO:canonical -->`, `<!-- SEO:og -->`, `<!-- SEO:schema -->`) для повторных прогонов.

---

## Task 1: Подготовить директорию и центральный список страниц

**Цель:** Заложить структуру `tools/seo/` и единый источник правды о страницах сайта (используется во всех последующих скриптах — DRY).

**Files:**
- Create: `tools/seo/PAGES.py`
- Create: `tools/seo/README.md`

- [ ] **Step 1: Создать директорию**

```bash
mkdir -p tools/seo
```

- [ ] **Step 2: Создать `tools/seo/PAGES.py` с единым списком страниц**

```python
"""Single source of truth: список production-страниц + их метаданные.

Используется генераторами sitemap, canonical/og, schema.org, smoke-тестами.
"""

# Каждая запись: (relative_path, public_url_path, page_type, title_for_og, description_for_og)
PAGES = [
    (
        "index.html", "/",
        "homepage",
        "Перманентный макияж в Москве — Студия Agent Permanent",
        "Студия и академия перманентного макияжа Арины Шараповой. Москва, м. Сокол. 6000+ процедур, 5.0 на Яндекс.Картах, 187 отзывов.",
    ),
    (
        "studio/index.html", "/studio/",
        "studio",
        "Студия перманентного макияжа в Москве — Agent Permanent",
        "Брови, губы, межресничка. Студия Agent Permanent у м. Сокол. Запись онлайн через Яндекс.Карты или сайт.",
    ),
    (
        "school/index.html", "/school/",
        "academy_hub",
        "Академия перманентного макияжа Agent Permanent",
        "Обучение мастеров перманентного макияжа от Арины Шараповой. Гибридный формат: теория онлайн + практика очно в Москве.",
    ),
    (
        "school/pm/index.html", "/school/pm/",
        "course_pm",
        "Обучение перманентному макияжу в Москве — Академия Agent Permanent",
        "Полный курс мастера ПМ: брови, губы, межресничка. Государственный диплом, 200+ выпускниц. Гибрид — теория онлайн + практика 5 дней в Москве.",
    ),
    (
        "school/brows/index.html", "/school/brows/",
        "course_brows",
        "Курс по перманентному макияжу бровей — Академия Agent Permanent",
        "Авторская методика Арины Шараповой: пудровое напыление, акварель, волосковая техника. Гибридный формат, диплом.",
    ),
    (
        "portfolio/index.html", "/portfolio/",
        "portfolio",
        "Портфолио работ — Студия Agent Permanent",
        "Реальные работы мастеров Agent Permanent: брови, губы, межресничка. Москва, 6000+ процедур, рейтинг 5.0 на Яндекс.Картах.",
    ),
    (
        "about/index.html", "/about/",
        "about",
        "О бренде Agent Permanent — Арина Шарапова",
        "История бренда: 10 лет работы, 200+ выпускниц-мастеров, 6000+ выполненных процедур. Студия в Москве и международная академия.",
    ),
    (
        "contacts/index.html", "/contacts/",
        "contacts",
        "Контакты студии Agent Permanent — Москва, м. Сокол",
        "Адрес: Москва, ул. Новопесчанная 6к2. Телефон, Telegram, WhatsApp, ВКонтакте, онлайн-запись через YClients.",
    ),
    (
        "privacy/index.html", "/privacy/",
        "legal",
        "Политика конфиденциальности — Agent Permanent",
        "Положения об обработке персональных данных в соответствии с 152-ФЗ.",
    ),
]

SITE_URL = "https://agentpermanent.ru"
SITE_BRAND = "Agent Permanent"
SITE_OWNER = "Арина Шарапова"
ADDRESS = {
    "streetAddress": "ул. Новопесчанная, 6к2",
    "addressLocality": "Москва",
    "addressRegion": "Москва",
    "postalCode": "125252",
    "addressCountry": "RU",
}
GEO = {"latitude": 55.801122, "longitude": 37.516653}
PHONE = "+7 (999) 820-04-21"
EMAIL = "ixar.sell@gmail.com"  # для контакт-данных Schema.org
OPENING_HOURS = "Mo-Su 10:00-21:00"
RATING = {"value": "5.0", "count": 187}
SAME_AS = [
    "https://t.me/+79998200421",
    "https://wa.me/79998200421",
    "https://vk.ru/agent_permanent",
    "https://www.instagram.com/sharapova.permanent",
    "https://yandex.com.tr/maps/org/agent_permanent/123024463523/",
]
LOGO_URL = SITE_URL + "/logo_2000x2000.png"
```

- [ ] **Step 3: Создать `tools/seo/README.md`**

```markdown
# SEO tools

Набор Python-скриптов для технического SEO. Все скрипты:
- идемпотентны (повторный запуск не дублирует тегов)
- читают единый список страниц из `PAGES.py`
- запускаются вручную перед деплоем

## Скрипты

| Скрипт | Что делает |
|---|---|
| `generate_sitemap.py` | пишет `sitemap.xml` в корень |
| `inject_canonical_og.py` | добавляет canonical + OpenGraph + Twitter Card в `<head>` |
| `inject_schema.py` | добавляет JSON-LD blocks (Organization, BeautySalon, Course, BreadcrumbList) |
| `update_titles.py` | переписывает `<title>` и `<meta name="description">` |
| `fix_alt_tags.py` | заполняет пустые `alt=""` в `portfolio/index.html` |
| `smoke_test.sh` | проверка что всё на месте (запуск перед деплоем) |
| `run_all.sh` | запускает все генераторы по порядку |

## Запуск

```bash
python3 tools/seo/run_all.sh   # обновить все теги
bash tools/seo/smoke_test.sh   # проверить
```
```

- [ ] **Step 4: Commit**

```bash
git add tools/seo/PAGES.py tools/seo/README.md
git commit -m "seo: tools/seo/ skeleton + central PAGES list"
```

---

## Task 2: Sitemap generator

**Цель:** Генерировать `sitemap.xml` из единого списка страниц, чтобы Я.Вебмастер видел все production-URL.

**Files:**
- Create: `tools/seo/generate_sitemap.py`
- Create: `sitemap.xml` (output)

- [ ] **Step 1: Написать `tools/seo/generate_sitemap.py`**

```python
#!/usr/bin/env python3
"""Генератор sitemap.xml из списка PAGES.

Запуск:  python3 tools/seo/generate_sitemap.py
Output:  sitemap.xml в корне проекта
"""
import os
import sys
import datetime
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(ROOT / "tools" / "seo"))
from PAGES import PAGES, SITE_URL  # noqa: E402


def file_lastmod(rel_path: str) -> str:
    """Возвращает дату последней модификации файла в формате YYYY-MM-DD."""
    p = ROOT / rel_path
    if not p.exists():
        # fallback to today
        return datetime.date.today().isoformat()
    ts = datetime.datetime.fromtimestamp(p.stat().st_mtime)
    return ts.date().isoformat()


def build_sitemap() -> str:
    lines = [
        '<?xml version="1.0" encoding="UTF-8"?>',
        '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
    ]
    for rel_path, url_path, _, _, _ in PAGES:
        url = SITE_URL + url_path
        lastmod = file_lastmod(rel_path)
        # Главная — приоритет 1.0, посадочные — 0.9, остальные — 0.7, privacy — 0.3
        if url_path == "/":
            priority = "1.0"
            changefreq = "weekly"
        elif "/school/" in url_path or url_path == "/studio/":
            priority = "0.9"
            changefreq = "weekly"
        elif url_path == "/privacy/":
            priority = "0.3"
            changefreq = "yearly"
        else:
            priority = "0.7"
            changefreq = "monthly"
        lines.append("  <url>")
        lines.append(f"    <loc>{url}</loc>")
        lines.append(f"    <lastmod>{lastmod}</lastmod>")
        lines.append(f"    <changefreq>{changefreq}</changefreq>")
        lines.append(f"    <priority>{priority}</priority>")
        lines.append("  </url>")
    lines.append("</urlset>")
    return "\n".join(lines) + "\n"


def main() -> int:
    out = build_sitemap()
    target = ROOT / "sitemap.xml"
    target.write_text(out, encoding="utf-8")

    # Self-check
    assert "<urlset" in out, "sitemap missing urlset"
    assert out.count("<url>") == len(PAGES), f"sitemap urls != PAGES count"
    print(f"✓ sitemap.xml: {len(PAGES)} URLs → {target}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Запустить генератор**

```bash
python3 tools/seo/generate_sitemap.py
```

Expected output:
```
✓ sitemap.xml: 9 URLs → /Users/.../sitemap.xml
```

- [ ] **Step 3: Проверить sitemap валидным XML**

```bash
python3 -c "import xml.etree.ElementTree as ET; ET.parse('sitemap.xml'); print('valid xml')"
```

Expected output: `valid xml`

- [ ] **Step 4: Проверить что sitemap содержит главную и /school/pm/**

```bash
grep -c "agentpermanent.ru/" sitemap.xml
```

Expected: `9`

```bash
grep "school/pm" sitemap.xml
```

Expected: строка с `<loc>https://agentpermanent.ru/school/pm/</loc>`

- [ ] **Step 5: Commit**

```bash
git add tools/seo/generate_sitemap.py sitemap.xml
git commit -m "seo: sitemap.xml generator + initial sitemap (9 URLs)"
```

---

## Task 3: robots.txt с указанием Sitemap

**Цель:** Заменить дефолтный CF-robots на свой, явно указав путь к sitemap, чтобы поисковики его нашли.

**Files:**
- Create: `robots.txt`

- [ ] **Step 1: Создать `robots.txt`**

```
User-agent: *
Allow: /

# Закрываем технические/служебные пути
Disallow: /tools/
Disallow: /docs/
Disallow: /.wrangler/
Disallow: /Видео/
Disallow: /Фото/Студия/
Disallow: /Фото/Гайды/
Disallow: /Фото/Баннеры/

# Закрываем ИИ-ботов от тренировки на нашем контенте
User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: Bytespider
Disallow: /

# Sitemap
Sitemap: https://agentpermanent.ru/sitemap.xml
```

- [ ] **Step 2: Проверить что файл создан**

```bash
ls -la robots.txt && head -5 robots.txt
```

- [ ] **Step 3: Commit**

```bash
git add robots.txt
git commit -m "seo: robots.txt with explicit Sitemap directive"
```

---

## Task 4: OpenGraph картинки 1200×630

**Цель:** Сделать 2 jpeg 1200×630 для красивых превью при шеринге сайта в TG/WA/VK. Одна для салонной части, одна для академии.

**Files:**
- Create: `og-image-studio.jpg`
- Create: `og-image-academy.jpg`

- [ ] **Step 1: Подобрать исходники**

```bash
ls -la hero_arina.jpg hero_photo_2.jpg school/banner_pm_01.jpg about/brand_studio.webp 2>/dev/null
```

Используем:
- `hero_arina.jpg` (521 KB) → og-image-studio.jpg (фото Арины — главное лицо бренда)
- `school/banner_pm_01.jpg` (1.2 MB) → og-image-academy.jpg (баннер курса ПМ)

- [ ] **Step 2: Создать og-image-studio.jpg (1200×630, центр-кроп)**

```bash
sips -Z 1500 hero_arina.jpg --out /tmp/og-studio-resize.jpg
sips -c 630 1200 /tmp/og-studio-resize.jpg --out og-image-studio.jpg
```

- [ ] **Step 3: Создать og-image-academy.jpg**

```bash
sips -Z 1500 school/banner_pm_01.jpg --out /tmp/og-academy-resize.jpg
sips -c 630 1200 /tmp/og-academy-resize.jpg --out og-image-academy.jpg
```

- [ ] **Step 4: Проверить размеры**

```bash
sips -g pixelHeight -g pixelWidth og-image-studio.jpg og-image-academy.jpg
```

Expected: pixelWidth: 1200 / pixelHeight: 630 для обоих файлов.

- [ ] **Step 5: Проверить размеры (должны быть < 300 KB каждый)**

```bash
ls -lh og-image-*.jpg
```

Expected: оба ≤ 300 KB (ОК для соцсетевых превью).

- [ ] **Step 6: Commit**

```bash
git add og-image-studio.jpg og-image-academy.jpg
git commit -m "seo: og-image 1200x630 for studio and academy share previews"
```

---

## Task 5: Inject canonical + OpenGraph + Twitter Card

**Цель:** На каждой production-странице добавить блок с canonical + og:* + twitter:* тегами. Идемпотентно.

**Files:**
- Create: `tools/seo/inject_canonical_og.py`
- Modify: все 9 production-страниц

- [ ] **Step 1: Написать `tools/seo/inject_canonical_og.py`**

```python
#!/usr/bin/env python3
"""Добавляет canonical + OpenGraph + Twitter Card в <head> каждой production-страницы.

Идемпотентно: при повторном запуске обновляет существующий блок (помеченный
HTML-комментарием SEO:canonical_og), а не дублирует его.
"""
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(ROOT / "tools" / "seo"))
from PAGES import PAGES, SITE_URL  # noqa: E402

START = "<!-- SEO:canonical_og:start -->"
END = "<!-- SEO:canonical_og:end -->"


def og_image_for(page_type: str) -> str:
    """Выбор og:image по типу страницы."""
    if page_type in ("course_pm", "course_brows", "academy_hub"):
        return SITE_URL + "/og-image-academy.jpg"
    return SITE_URL + "/og-image-studio.jpg"


def og_type_for(page_type: str) -> str:
    if page_type in ("course_pm", "course_brows"):
        return "website"  # есть Course schema, но og:type оставляем website
    return "website"


def build_block(rel_path: str, url_path: str, page_type: str, title: str, description: str) -> str:
    canonical_url = SITE_URL + url_path
    og_image = og_image_for(page_type)
    og_type = og_type_for(page_type)
    return f"""{START}
  <link rel="canonical" href="{canonical_url}">
  <meta property="og:type" content="{og_type}">
  <meta property="og:site_name" content="Agent Permanent">
  <meta property="og:locale" content="ru_RU">
  <meta property="og:url" content="{canonical_url}">
  <meta property="og:title" content="{title}">
  <meta property="og:description" content="{description}">
  <meta property="og:image" content="{og_image}">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{title}">
  <meta name="twitter:description" content="{description}">
  <meta name="twitter:image" content="{og_image}">
  {END}"""


def inject(rel_path: str, block: str) -> bool:
    """Вставляет блок перед </head>. Если уже был — заменяет.
    Возвращает True если файл изменился."""
    path = ROOT / rel_path
    if not path.exists():
        print(f"  ⚠  {rel_path} not found — skip")
        return False
    content = path.read_text(encoding="utf-8")

    # Если уже есть наш блок — удаляем (чтобы заменить)
    if START in content and END in content:
        pattern = re.compile(re.escape(START) + r".*?" + re.escape(END), re.DOTALL)
        content = pattern.sub("__SEO_PLACEHOLDER__", content)
        new_content = content.replace("__SEO_PLACEHOLDER__", block)
    else:
        # Вставляем перед </head>
        if "</head>" not in content:
            print(f"  ⚠  {rel_path} no </head> — skip")
            return False
        new_content = content.replace("</head>", f"  {block}\n</head>", 1)

    if new_content != path.read_text(encoding="utf-8"):
        path.write_text(new_content, encoding="utf-8")
        return True
    return False


def main() -> int:
    changed = 0
    for rel_path, url_path, page_type, title, description in PAGES:
        block = build_block(rel_path, url_path, page_type, title, description)
        if inject(rel_path, block):
            print(f"  ✓ {rel_path}")
            changed += 1
        else:
            print(f"    {rel_path} (no change)")
    print(f"\n{changed}/{len(PAGES)} pages updated.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Запустить скрипт**

```bash
python3 tools/seo/inject_canonical_og.py
```

Expected output:
```
  ✓ index.html
  ✓ studio/index.html
  ✓ school/index.html
  ✓ school/pm/index.html
  ✓ school/brows/index.html
  ✓ portfolio/index.html
  ✓ about/index.html
  ✓ contacts/index.html
  ✓ privacy/index.html

9/9 pages updated.
```

- [ ] **Step 3: Проверить вставку на главной**

```bash
grep -A 1 "rel=\"canonical\"" index.html | head -2
```

Expected: `<link rel="canonical" href="https://agentpermanent.ru/">`

```bash
grep "og:title" index.html
```

Expected: `<meta property="og:title" content="Перманентный макияж в Москве — Студия Agent Permanent">`

- [ ] **Step 4: Проверить идемпотентность (повторный запуск не дублирует)**

```bash
python3 tools/seo/inject_canonical_og.py
grep -c "SEO:canonical_og:start" index.html
```

Expected: `1` (ровно один блок, не два).

- [ ] **Step 5: Commit**

```bash
git add tools/seo/inject_canonical_og.py index.html studio/index.html school/index.html school/pm/index.html school/brows/index.html portfolio/index.html about/index.html contacts/index.html privacy/index.html
git commit -m "seo: canonical + OpenGraph + Twitter Card on all 9 pages"
```

---

## Task 6: Inject Schema.org JSON-LD

**Цель:** На каждой production-странице добавить JSON-LD с правильным типом (BeautySalon, Course, Organization, BreadcrumbList).

**Files:**
- Create: `tools/seo/inject_schema.py`
- Modify: все 9 production-страниц

- [ ] **Step 1: Написать `tools/seo/inject_schema.py`**

```python
#!/usr/bin/env python3
"""Добавляет JSON-LD блоки в <head> по типам страниц.

Маппинг типа страницы → набор schema-блоков задан в SCHEMA_MAP.
Идемпотентно (повторный запуск обновляет существующий блок).
"""
import json
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(ROOT / "tools" / "seo"))
from PAGES import (  # noqa: E402
    PAGES, SITE_URL, SITE_BRAND, SITE_OWNER,
    ADDRESS, GEO, PHONE, OPENING_HOURS, RATING, SAME_AS, LOGO_URL,
)

START = "<!-- SEO:schema:start -->"
END = "<!-- SEO:schema:end -->"


def organization() -> dict:
    return {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": SITE_BRAND,
        "alternateName": "Agent Permanent by Arina Sharapova",
        "url": SITE_URL + "/",
        "logo": LOGO_URL,
        "founder": {"@type": "Person", "name": SITE_OWNER},
        "address": {"@type": "PostalAddress", **ADDRESS},
        "telephone": PHONE,
        "sameAs": SAME_AS,
    }


def beauty_salon(with_rating: bool) -> dict:
    out = {
        "@context": "https://schema.org",
        "@type": "BeautySalon",
        "name": SITE_BRAND,
        "image": SITE_URL + "/og-image-studio.jpg",
        "@id": SITE_URL + "/#salon",
        "url": SITE_URL + "/",
        "telephone": PHONE,
        "priceRange": "₽₽",
        "address": {"@type": "PostalAddress", **ADDRESS},
        "geo": {"@type": "GeoCoordinates", **GEO},
        "openingHoursSpecification": [{
            "@type": "OpeningHoursSpecification",
            "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"],
            "opens": "10:00",
            "closes": "21:00",
        }],
        "sameAs": SAME_AS,
    }
    if with_rating:
        out["aggregateRating"] = {
            "@type": "AggregateRating",
            "ratingValue": RATING["value"],
            "reviewCount": RATING["count"],
            "bestRating": "5",
            "worstRating": "1",
        }
    return out


def course(name: str, description: str, course_url: str) -> dict:
    return {
        "@context": "https://schema.org",
        "@type": "Course",
        "name": name,
        "description": description,
        "url": SITE_URL + course_url,
        "provider": {
            "@type": "EducationalOrganization",
            "name": "Академия Agent Permanent",
            "url": SITE_URL + "/school/",
            "sameAs": SAME_AS,
        },
        "educationalCredentialAwarded": "Государственный диплом мастера ПМ",
        "courseMode": "blended",
        "inLanguage": "ru",
    }


def educational_organization() -> dict:
    return {
        "@context": "https://schema.org",
        "@type": "EducationalOrganization",
        "name": "Академия Agent Permanent",
        "url": SITE_URL + "/school/",
        "logo": LOGO_URL,
        "address": {"@type": "PostalAddress", **ADDRESS},
        "sameAs": SAME_AS,
        "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": RATING["value"],
            "reviewCount": RATING["count"],
            "bestRating": "5",
            "worstRating": "1",
        },
    }


def breadcrumb(items: list) -> dict:
    return {
        "@context": "https://schema.org",
        "@type": "BreadcrumbList",
        "itemListElement": [
            {
                "@type": "ListItem",
                "position": i + 1,
                "name": name,
                "item": SITE_URL + url,
            }
            for i, (name, url) in enumerate(items)
        ],
    }


# Маппинг: тип страницы → список schema-объектов
SCHEMA_MAP = {
    "homepage": lambda: [
        beauty_salon(with_rating=True),
        organization(),
    ],
    "studio": lambda: [
        beauty_salon(with_rating=True),
        breadcrumb([("Главная", "/"), ("Студия", "/studio/")]),
    ],
    "academy_hub": lambda: [
        educational_organization(),
        breadcrumb([("Главная", "/"), ("Академия", "/school/")]),
    ],
    "course_pm": lambda: [
        course(
            name="Курс перманентного макияжа",
            description="Полный базовый курс мастера ПМ: брови, губы, межресничка. Государственный диплом, гибридный формат — теория онлайн, практика очно в Москве.",
            course_url="/school/pm/",
        ),
        breadcrumb([("Главная", "/"), ("Академия", "/school/"), ("Курс ПМ", "/school/pm/")]),
    ],
    "course_brows": lambda: [
        course(
            name="Курс по перманентному макияжу бровей",
            description="Авторская методика Арины Шараповой: пудровое напыление, акварель, волосковая техника. Гибридный формат, диплом.",
            course_url="/school/brows/",
        ),
        breadcrumb([("Главная", "/"), ("Академия", "/school/"), ("Курс по бровям", "/school/brows/")]),
    ],
    "portfolio": lambda: [
        beauty_salon(with_rating=True),
        breadcrumb([("Главная", "/"), ("Портфолио", "/portfolio/")]),
    ],
    "about": lambda: [
        organization(),
        breadcrumb([("Главная", "/"), ("О бренде", "/about/")]),
    ],
    "contacts": lambda: [
        beauty_salon(with_rating=True),
        breadcrumb([("Главная", "/"), ("Контакты", "/contacts/")]),
    ],
    "legal": lambda: [
        breadcrumb([("Главная", "/"), ("Конфиденциальность", "/privacy/")]),
    ],
}


def build_block(page_type: str) -> str:
    schemas = SCHEMA_MAP[page_type]()
    blocks = []
    for s in schemas:
        body = json.dumps(s, ensure_ascii=False, indent=2)
        blocks.append(f'<script type="application/ld+json">\n{body}\n</script>')
    inner = "\n".join(blocks)
    return f"{START}\n{inner}\n{END}"


def inject(rel_path: str, block: str) -> bool:
    path = ROOT / rel_path
    if not path.exists():
        print(f"  ⚠  {rel_path} not found")
        return False
    content = path.read_text(encoding="utf-8")
    if START in content and END in content:
        pattern = re.compile(re.escape(START) + r".*?" + re.escape(END), re.DOTALL)
        content = pattern.sub("__SCHEMA_PH__", content)
        new_content = content.replace("__SCHEMA_PH__", block)
    else:
        if "</head>" not in content:
            return False
        new_content = content.replace("</head>", f"  {block}\n</head>", 1)
    if new_content != path.read_text(encoding="utf-8"):
        path.write_text(new_content, encoding="utf-8")
        return True
    return False


def main() -> int:
    changed = 0
    for rel_path, url_path, page_type, _, _ in PAGES:
        if page_type not in SCHEMA_MAP:
            print(f"  ⚠  unknown page_type {page_type}, skip")
            continue
        block = build_block(page_type)
        if inject(rel_path, block):
            print(f"  ✓ {rel_path}")
            changed += 1
        else:
            print(f"    {rel_path} (no change)")
    print(f"\n{changed}/{len(PAGES)} pages updated.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Запустить**

```bash
python3 tools/seo/inject_schema.py
```

Expected: `9/9 pages updated.`

- [ ] **Step 3: Проверить главную**

```bash
grep -c '"@type": "BeautySalon"' index.html
```

Expected: `1`

```bash
grep -c '"aggregateRating"' index.html
```

Expected: `1`

- [ ] **Step 4: Проверить курс**

```bash
grep -c '"@type": "Course"' school/pm/index.html
```

Expected: `1`

- [ ] **Step 5: Проверить идемпотентность**

```bash
python3 tools/seo/inject_schema.py
grep -c "SEO:schema:start" index.html
```

Expected: `1`

- [ ] **Step 6: Валидация JSON-LD через Python**

```bash
python3 -c "
import re, json
content = open('index.html').read()
blocks = re.findall(r'<script type=\"application/ld\+json\">(.*?)</script>', content, re.DOTALL)
for b in blocks:
    parsed = json.loads(b)
    print(parsed.get('@type'), 'OK')
"
```

Expected:
```
BeautySalon OK
Organization OK
```

- [ ] **Step 7: Commit**

```bash
git add tools/seo/inject_schema.py index.html studio/index.html school/index.html school/pm/index.html school/brows/index.html portfolio/index.html about/index.html contacts/index.html privacy/index.html
git commit -m "seo: Schema.org JSON-LD (BeautySalon, Course, Organization, BreadcrumbList) on all 9 pages"
```

---

## Task 7: Update titles и descriptions под локальные ключи

**Цель:** Обновить `<title>` и `<meta name="description">` на ключевых страницах под локальные коммерческие запросы (Москва, Сокол).

**Files:**
- Create: `tools/seo/update_titles.py`
- Modify: 9 production-страниц

- [ ] **Step 1: Написать `tools/seo/update_titles.py`**

```python
#!/usr/bin/env python3
"""Заменяет существующие <title> и <meta name="description"> на оптимизированные
под локальные ключи. Берёт значения из PAGES.

Идемпотентно: при повторном запуске не плодит лишние теги, просто перезаписывает.
"""
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(ROOT / "tools" / "seo"))
from PAGES import PAGES  # noqa: E402


def replace_title(content: str, new_title: str) -> str:
    return re.sub(
        r"<title>.*?</title>",
        f"<title>{new_title}</title>",
        content,
        count=1,
        flags=re.DOTALL,
    )


def replace_description(content: str, new_description: str) -> str:
    pattern = re.compile(
        r'<meta\s+name="description"\s+content="[^"]*"\s*/?>',
        re.IGNORECASE,
    )
    new_meta = f'<meta name="description" content="{new_description}">'
    if pattern.search(content):
        return pattern.sub(new_meta, content, count=1)
    # если description нет — вставим перед </head>
    return content.replace("</head>", f"  {new_meta}\n</head>", 1)


def main() -> int:
    changed = 0
    for rel_path, _, _, title, description in PAGES:
        path = ROOT / rel_path
        if not path.exists():
            continue
        content = path.read_text(encoding="utf-8")
        original = content
        content = replace_title(content, title)
        content = replace_description(content, description)
        if content != original:
            path.write_text(content, encoding="utf-8")
            print(f"  ✓ {rel_path}")
            changed += 1
    print(f"\n{changed}/{len(PAGES)} pages updated.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Запустить**

```bash
python3 tools/seo/update_titles.py
```

- [ ] **Step 3: Проверить**

```bash
grep -A 0 "<title>" index.html | head -1
```

Expected: `  <title>Перманентный макияж в Москве — Студия Agent Permanent</title>`

```bash
grep "name=\"description\"" school/pm/index.html | head -1
```

Expected: содержит "Полный курс мастера ПМ" и "Москве".

- [ ] **Step 4: Идемпотентность**

```bash
python3 tools/seo/update_titles.py
grep -c "<title>" index.html
```

Expected: `1` (ровно один title).

- [ ] **Step 5: Commit**

```bash
git add tools/seo/update_titles.py index.html studio/index.html school/index.html school/pm/index.html school/brows/index.html portfolio/index.html about/index.html contacts/index.html privacy/index.html
git commit -m "seo: localized title + description (Moscow + Sokol) on all 9 pages"
```

---

## Task 8: Заполнить пустые alt-теги в галерее портфолио

**Цель:** Сейчас в `portfolio/index.html` 173 пустых `alt=""` — это огромный потерянный SEO-актив (Яндекс.Картинки активно индексируют beauty). Сгенерируем по шаблону на основе пути файла.

**Files:**
- Create: `tools/seo/fix_alt_tags.py`
- Modify: `portfolio/index.html`

- [ ] **Step 1: Написать `tools/seo/fix_alt_tags.py`**

```python
#!/usr/bin/env python3
"""Заполняет пустые alt="" в portfolio/index.html осмысленным текстом
на основе пути файла.

Шаблон:  "<Услуга в им.падеже> — работа <Мастер>, Agent Permanent, Москва"

Пример пути: ../Фото/Работы/Арина/Брови/img_1474.png
  → услуга: брови → "Перманент бровей"
  → мастер: Арина → "Арина Шарапова"
  → alt:    "Перманент бровей — работа Арины Шараповой, Agent Permanent, Москва"
"""
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent

# словарь "путь→название услуги в человеческом виде"
SERVICE_BY_FOLDER = {
    "Брови": "Перманент бровей",
    "Губы": "Перманент губ",
    "Все лицо": "Перманентный макияж",
    "Межресничка": "Перманентный макияж — межресничка",
    "Удаления": "Удаление перманентного макияжа",
    "Удаление": "Удаление перманентного макияжа",
}

# словарь "имя в пути → ФИО мастера"
MASTER_BY_FOLDER = {
    "Арина": "Арины Шараповой",
    "Вика": "мастера Виктории",
    "Ученики": "мастеров Академии Agent Permanent",
}


def alt_for(src: str) -> str:
    # src вида "../Фото/Работы/Арина/Брови/img_1474.png"
    # делим по / и ищем известные сегменты
    parts = src.split("/")

    master = None
    service = None
    for i, p in enumerate(parts):
        if p in MASTER_BY_FOLDER:
            master = MASTER_BY_FOLDER[p]
        if p in SERVICE_BY_FOLDER:
            service = SERVICE_BY_FOLDER[p]

    if not service:
        service = "Перманентный макияж"
    if not master:
        master = "Арины Шараповой"

    return f"{service} — работа {master}, Agent Permanent, Москва"


def main() -> int:
    target = ROOT / "portfolio" / "index.html"
    if not target.exists():
        print(f"  ⚠  {target} not found")
        return 1

    content = target.read_text(encoding="utf-8")

    # Находим все <img ... src="..." ... alt="" ...> с пустыми alt
    # и заполняем их.
    # Тег <img>: src и alt могут идти в любом порядке. Используем
    # двухпроходный подход: ищем все <img>, парсим атрибуты.

    pattern = re.compile(r'<img\s+([^>]*?)>', re.DOTALL)
    fixed = 0

    def replace_img(match: re.Match) -> str:
        nonlocal fixed
        attrs = match.group(1)
        src_match = re.search(r'src="([^"]+)"', attrs)
        alt_match = re.search(r'alt="([^"]*)"', attrs)
        if not src_match or not alt_match:
            return match.group(0)
        # обновляем только пустые alt
        if alt_match.group(1).strip() != "":
            return match.group(0)
        new_alt = alt_for(src_match.group(1))
        new_attrs = re.sub(r'alt="[^"]*"', f'alt="{new_alt}"', attrs, count=1)
        fixed += 1
        return f'<img {new_attrs}>'

    new_content = pattern.sub(replace_img, content)

    if new_content != content:
        target.write_text(new_content, encoding="utf-8")
    print(f"  ✓ portfolio/index.html: {fixed} alt-tags filled")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Создать unit-тест функции `alt_for()`**

```bash
python3 -c "
import sys
sys.path.insert(0, 'tools/seo')
from fix_alt_tags import alt_for

tests = [
    ('../Фото/Работы/Арина/Брови/img_1474.png',
     'Перманент бровей — работа Арины Шараповой, Agent Permanent, Москва'),
    ('../Фото/Работы/Арина/Губы/img_1483.png',
     'Перманент губ — работа Арины Шараповой, Agent Permanent, Москва'),
    ('../Фото/Работы/Вика/Брови/img_4430.png',
     'Перманент бровей — работа мастера Виктории, Agent Permanent, Москва'),
    ('../Фото/Работы/Удаления/img_x.png',
     'Удаление перманентного макияжа — работа Арины Шараповой, Agent Permanent, Москва'),
    ('../Фото/Работы/Ученики/img_y.png',
     'Перманентный макияж — работа мастеров Академии Agent Permanent, Agent Permanent, Москва'),
]
for src, expected in tests:
    got = alt_for(src)
    assert got == expected, f'FAIL\n  src:      {src}\n  expected: {expected}\n  got:      {got}'
    print('  PASS', src)
print('All tests passed')
"
```

Expected: 5 PASS строк + "All tests passed".

- [ ] **Step 3: Запустить скрипт**

```bash
python3 tools/seo/fix_alt_tags.py
```

Expected: `✓ portfolio/index.html: 173 alt-tags filled` (число может отличаться, главное >100).

- [ ] **Step 4: Проверить что пустых alt стало мало**

```bash
grep -oE 'alt="[^"]*"' portfolio/index.html | sort | uniq -c | sort -rn | head -5
```

Expected: топ-3 строк — заполненные alt с услугой и мастером, не `alt=""`.

```bash
grep -c 'alt=""' portfolio/index.html
```

Expected: `0` (или близко к нулю).

- [ ] **Step 5: Идемпотентность (повторный запуск ничего не меняет)**

```bash
python3 tools/seo/fix_alt_tags.py
```

Expected: `0 alt-tags filled` (все уже заполнены).

- [ ] **Step 6: Commit**

```bash
git add tools/seo/fix_alt_tags.py portfolio/index.html
git commit -m "seo: fill 173 empty alt tags in portfolio with master+service text"
```

---

## Task 9: Smoke-тест и run_all.sh

**Цель:** Один скрипт для запуска всех генераторов и один для smoke-проверки. Это упрощает поддержку и предотвращает регрессии.

**Files:**
- Create: `tools/seo/run_all.sh`
- Create: `tools/seo/smoke_test.sh`

- [ ] **Step 1: Создать `tools/seo/run_all.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../.."

echo "=== SEO: regenerate all ==="
python3 tools/seo/generate_sitemap.py
python3 tools/seo/inject_canonical_og.py
python3 tools/seo/inject_schema.py
python3 tools/seo/update_titles.py
python3 tools/seo/fix_alt_tags.py
echo "=== Done ==="
```

- [ ] **Step 2: Создать `tools/seo/smoke_test.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../.."

PAGES=(
  "index.html"
  "studio/index.html"
  "school/index.html"
  "school/pm/index.html"
  "school/brows/index.html"
  "portfolio/index.html"
  "about/index.html"
  "contacts/index.html"
  "privacy/index.html"
)

errors=0

check() {
  local file="$1"
  local pattern="$2"
  local label="$3"
  if ! grep -q "$pattern" "$file"; then
    echo "  ✗ $file: missing $label"
    errors=$((errors + 1))
  fi
}

echo "=== sitemap.xml ==="
[[ -f sitemap.xml ]] && echo "  ✓ exists" || { echo "  ✗ missing"; errors=$((errors+1)); }

echo "=== robots.txt ==="
grep -q "Sitemap: https://agentpermanent.ru/sitemap.xml" robots.txt || { echo "  ✗ no Sitemap directive"; errors=$((errors+1)); }
echo "  ✓ Sitemap directive present"

echo ""
echo "=== Per-page checks ==="
for f in "${PAGES[@]}"; do
  echo "$f"
  check "$f" 'rel="canonical"'              "canonical"
  check "$f" 'property="og:title"'          "og:title"
  check "$f" 'property="og:description"'    "og:description"
  check "$f" 'property="og:image"'          "og:image"
  check "$f" 'name="twitter:card"'          "twitter:card"
  check "$f" 'application/ld+json'          "JSON-LD schema"
done

echo ""
echo "=== portfolio empty-alt ==="
empty=$(grep -c 'alt=""' portfolio/index.html || true)
if [[ "$empty" -gt 5 ]]; then
  echo "  ✗ portfolio: $empty empty alt tags (expected ≤ 5)"
  errors=$((errors + 1))
else
  echo "  ✓ portfolio: $empty empty alt tags"
fi

echo ""
if [[ "$errors" -eq 0 ]]; then
  echo "✅ All SEO smoke tests passed."
  exit 0
else
  echo "❌ $errors errors."
  exit 1
fi
```

- [ ] **Step 3: Сделать скрипты исполняемыми**

```bash
chmod +x tools/seo/run_all.sh tools/seo/smoke_test.sh
```

- [ ] **Step 4: Прогнать smoke-тест**

```bash
bash tools/seo/smoke_test.sh
```

Expected: финальная строка `✅ All SEO smoke tests passed.` и exit code 0.

- [ ] **Step 5: Прогнать run_all (должен быть идемпотентным — изменений нет)**

```bash
bash tools/seo/run_all.sh
git status
```

Expected: `git status` показывает чистое состояние (`nothing to commit`) — все генераторы идемпотентны.

- [ ] **Step 6: Commit**

```bash
git add tools/seo/run_all.sh tools/seo/smoke_test.sh
git commit -m "seo: run_all + smoke_test scripts (CI-ready guard against regressions)"
```

---

## Task 10: Runbook для Яндекс.Бизнес (для Игоря)

**Цель:** Пошаговая инструкция, что Игорю сделать руками в карточке Я.Бизнес.

**Files:**
- Create: `docs/superpowers/runbooks/yandex-business-setup.md`

- [ ] **Step 1: Написать runbook**

```markdown
# Runbook — настройка карточки Яндекс.Бизнес

**Цель:** довести карточку студии Agent Permanent до состояния "полная витрина" — это даст ~70% быстрых заявок из локальной выдачи Яндекса.

**Время:** 2–3 часа разово + 1–2 часа в неделю на поддержку.

**Доступы:** есть у Игоря.

---

## Шаг 1. Открыть карточку

1. Зайти на https://yandex.ru/sprav/
2. Найти карточку **Agent Permanent** (адрес: Москва, ул. Новопесчанная 6к2).
3. Если карточка показывается серым/«не подтверждена» — пройти верификацию (звонок робота на телефон в карточке).

## Шаг 2. Заполнить базу

В разделе **«Информация → Основное»**:

- ✅ Название: `Agent Permanent`
- ✅ Категория основная: **Салон красоты** (если есть — то «Перманентный макияж»)
- ✅ Категории дополнительные: `Студия татуажа`, `Учебный центр`, `Курсы перманентного макияжа`
- ✅ Краткое описание (до 150 символов): `Студия и академия перманентного макияжа в Москве. Брови, губы, межресничка. 6000+ процедур, 5.0/187 отзывов.`
- ✅ Полное описание (до 1000 символов): пиши о бренде, опыте Арины, гарантиях, гибридном обучении. Обязательно упомянуть: «перманентный макияж», «брови», «губы», «межресничка», «м. Сокол», «Москва», «академия», «обучение мастера».

В разделе **«Контакты»**:

- ✅ Телефон: +7 (999) 820-04-21
- ✅ WhatsApp / Telegram — добавить
- ✅ Сайт: https://agentpermanent.ru
- ✅ ВКонтакте: https://vk.ru/agent_permanent
- ✅ Instagram: @sharapova.permanent

В разделе **«Часы работы»**:

- ✅ Пн–Вс 10:00–21:00 (без выходных)

## Шаг 3. Услуги (это критично для SEO)

Раздел **«Прайс-лист и услуги»**:

Создать 5 услуг — **отдельная карточка на каждую**:

| Название услуги | Краткое описание (до 200 знаков) |
|---|---|
| Перманентный макияж бровей | Пудровое напыление, акварельная и волосковая техника. Натуральный результат на 1.5–3 года. Студия Agent Permanent у м. Сокол. |
| Перманентный макияж губ | Натуральное окрашивание, контур с растушёвкой, акварельная техника. Стойкий результат на 2–4 года. |
| Межресничка (стрелки) | Перманентный макияж межресничного пространства. Эффект густых ресниц 24/7, без стрелок и со стрелками. |
| Удаление перманентного макияжа | Лазерное удаление и ремувер. Подбираем метод по типу пигмента и кожи. |
| Коррекция перманентного макияжа | Обновление цвета и формы перманента. Доводка после первичной процедуры. |

К каждой услуге:
- 3–5 фото из портфолио (берём из `Фото/Работы/Арина/...`).
- Цена (если решим публиковать) или «по запросу».

## Шаг 4. Фото

Раздел **«Фото»**:

- Загрузить **30–50 фото работ** (берём из `Фото/Работы/Арина/Брови/`, `/Губы/`, `/Все лицо/`).
- Загрузить **5–10 фото интерьера студии** (если есть в `Фото/Студия/`).
- Загрузить **фото мастеров** (Арина, Вика).

Качество: 1024×1024 минимум, JPG. Подписи к фото — это тоже SEO-сигнал, в подпись добавляем «брови до и после», «перманент губ натуральный эффект» и т.д.

## Шаг 5. Посты (как Stories)

Раздел **«Посты»**:

- Включить функцию.
- Публиковать **1–2 поста в неделю**.
- Темы:
  - Новые работы с фото до/после
  - Истории клиентов («Анна делала перманент бровей, и через год пришла на коррекцию»)
  - Анонс набора на курс
  - Полезные советы («Как ухаживать за перманентом первые 14 дней»)

Пост должен включать **ключевые слова** (брови, перманент, Москва, Сокол).

## Шаг 6. Отзывы

Раздел **«Отзывы»**:

- Отвечать на **все** отзывы в течение 24 часов.
- В ответе обязательно: имя клиента + услуга + повтор названия студии. Пример:
  > Анна, спасибо за тёплые слова! Рады, что вам понравились брови. Будем ждать вас на коррекции через 1.5–2 года в студии Agent Permanent.

Это **сильный поведенческий сигнал** для Яндекса — карточки с быстрыми ответами поднимаются в выдаче.

## Шаг 7. Онлайн-запись

Раздел **«Онлайн-запись»**:

- Подключить **YClients** (n890272.yclients.com — наш аккаунт).
- В карточке появится кнопка «Записаться» с прямым переходом в виджет.

## Шаг 8. Расписание (для академии)

Если в Я.Бизнес есть тип карточки «Учебный центр» — создать **отдельную карточку** для академии (по тому же адресу, но с типом «учебный центр»). Это даст вторую точку входа из локальной выдачи.

---

## Поддержка (раз в неделю)

- ☐ Опубликовать 1–2 поста (новые работы или истории).
- ☐ Ответить на новые отзывы (все, без исключения).
- ☐ Залить 1–3 новых фото работ.
- ☐ Раз в месяц обновить акции, если есть.
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/runbooks/yandex-business-setup.md
git commit -m "docs: yandex-business-setup runbook for Stage 1"
```

---

## Task 11: Runbook для Яндекс.Вебмастер

**Цель:** Регистрация сайта в Я.Вебмастере, подача sitemap, верификация владения.

**Files:**
- Create: `docs/superpowers/runbooks/yandex-webmaster-setup.md`

- [ ] **Step 1: Написать runbook**

```markdown
# Runbook — регистрация в Яндекс.Вебмастере

**Цель:** научить Яндекс целенаправленно индексировать сайт + получать статистику запросов и позиций.

**Время:** ~30 минут разово.

**Аккаунт:** тот же Yandex-аккаунт, что для Метрики (счётчик 109018359).

---

## Шаг 1. Добавить сайт

1. Открыть https://webmaster.yandex.ru/
2. Войти Яндекс-аккаунтом (тот же, что для Метрики).
3. Кнопка **«+ Добавить сайт»**.
4. Ввести: `https://agentpermanent.ru`
5. **Continue**.

## Шаг 2. Подтвердить права

Самый простой способ — **через мета-тег**.

1. Я.Вебмастер выдаст строку вида:
   ```
   <meta name="yandex-verification" content="abcdef1234567890">
   ```
2. **Скопировать `content`-значение** (хеш) и **прислать его Игорю-разработчику** (Claude). Я добавлю мета-тег во все 9 production-страниц через PAGES.py + новый скрипт inject_verifications.py.
3. После деплоя нажать в Я.Вебмастере **«Проверить»**.

**Альтернатива (если не хочется ждать деплоя):** загрузить файл с уникальным именем в корень сайта (Я.Вебмастер выдаст). Я могу создать этот файл сразу.

## Шаг 3. Добавить sitemap

1. После верификации — раздел **«Индексирование → Файлы Sitemap»**.
2. Кнопка **«Добавить файл Sitemap»**.
3. Ввести: `https://agentpermanent.ru/sitemap.xml`
4. **Добавить**.

В течение 1–7 дней Яндекс пройдётся по всем URL и начнёт индексировать.

## Шаг 4. Связать с Метрикой

1. Раздел **«Привязка к Я.Метрике»**.
2. Выбрать счётчик **109018359** (Agent Permanent).
3. **Привязать**.

Это даст возможность видеть в Метрике поисковые запросы (какие фразы привели на сайт) и в Вебмастере — поведенческие.

## Шаг 5. Регион

Раздел **«Информация о сайте → Региональность»**:

- Главный регион: **Москва**.
- Это критично для локальной выдачи студии.

## Шаг 6. Турбо-страницы (опционально)

Можно подключить Яндекс.Турбо позже, если будет блог — это даёт мгновенно загружающиеся версии для мобильных. Сейчас не критично.

---

## Что мониторить раз в неделю

В Я.Вебмастере:

- **Индексирование** — все ли страницы из sitemap проиндексированы.
- **Поисковые запросы** — по каким фразам нас уже находят (тут возникнет семядро для Этапа 2).
- **Ошибки** — 4xx, 5xx, дубли.
- **Внешние ссылки** — кто на нас сослался (полезно для SMM-стратегии).
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/runbooks/yandex-webmaster-setup.md
git commit -m "docs: yandex-webmaster-setup runbook for Stage 1"
```

---

## Task 12: Финальный деплой и валидация на проде

**Цель:** Залить все наработки на прод и убедиться что Яндекс/Google видят корректные мета-данные.

**Files:** (только деплой, новых файлов нет)

- [ ] **Step 1: Прогнать smoke-тест локально**

```bash
bash tools/seo/smoke_test.sh
```

Expected: `✅ All SEO smoke tests passed.`

Если тест провален — исправить выявленные проблемы и повторить.

- [ ] **Step 2: Bump CSS version (cache-bust)**

```bash
python3 -c "
import re, os
NEW = '2026-05-03b'
for root, dirs, files in os.walk('.'):
    if any(skip in root for skip in ['.git', '.claude', 'node_modules', '_files', 'worktrees']):
        continue
    for f in files:
        if not f.endswith('.html'): continue
        p = os.path.join(root, f)
        with open(p, 'r', encoding='utf-8') as fh: c = fh.read()
        new = re.sub(r'(style\.css\?v=)[0-9a-zA-Z-]+', rf'\g<1>{NEW}', c)
        if new != c:
            with open(p, 'w', encoding='utf-8') as fh: fh.write(new)
"
```

- [ ] **Step 3: Деплой на Cloudflare Pages**

```bash
npx wrangler pages deploy . --project-name=agent-permanent --branch=main --commit-dirty=true --commit-message="seo stage 1: sitemap + schema + og + alts" 2>&1 | tail -5
```

Expected: `✨ Deployment complete!`

- [ ] **Step 4: Push commits на GitHub**

```bash
git push origin main 2>&1 | tail -3
```

- [ ] **Step 5: Прогнать валидаторы на проде**

```bash
echo "=== sitemap живой? ===" && curl -sI https://agentpermanent.ru/sitemap.xml | head -3

echo "=== robots живой? ===" && curl -s https://agentpermanent.ru/robots.txt | grep Sitemap

echo "=== schema.org на главной ===" && curl -s https://agentpermanent.ru/ | grep -c "application/ld+json"

echo "=== canonical на главной ===" && curl -s https://agentpermanent.ru/ | grep -oE 'rel="canonical"[^>]*'

echo "=== og:image на главной ===" && curl -s https://agentpermanent.ru/ | grep "og:image"

echo "=== og-image файл доступен? ===" && curl -sI https://agentpermanent.ru/og-image-studio.jpg | head -2
```

Expected:
- sitemap.xml — `HTTP/2 200`, `content-type: application/xml`
- robots.txt — строка `Sitemap: https://agentpermanent.ru/sitemap.xml`
- schema на главной — `2` (BeautySalon + Organization)
- canonical — есть
- og:image — указывает на `/og-image-studio.jpg`
- og-image-studio.jpg — `HTTP/2 200`

- [ ] **Step 6: Schema.org валидация (вручную)**

Открой в браузере (это тебе вручную):
1. https://validator.schema.org/#url=https%3A%2F%2Fagentpermanent.ru%2F
   — должна быть `BeautySalon` + `Organization`, без ошибок.
2. https://validator.schema.org/#url=https%3A%2F%2Fagentpermanent.ru%2Fschool%2Fpm%2F
   — должна быть `Course`, без ошибок.

- [ ] **Step 7: OpenGraph валидация (вручную)**

Открой в браузере:
1. https://www.opengraph.xyz/url/https%3A%2F%2Fagentpermanent.ru
   — должно показать карточку с фото, заголовком, описанием.
2. Поделиться ссылкой `https://agentpermanent.ru` в любой Telegram-чат — должна появиться превью с большой картинкой.

- [ ] **Step 8: Финальный тег-snapshot**

```bash
git tag -a "seo-stage-1-complete" -m "SEO Stage 1 done: sitemap, robots, schema.org, og, canonical, alts, runbooks"
git push origin "seo-stage-1-complete"
```

---

## Self-Review

После того как агент закончил все 12 задач, прогон:

1. ✅ **Spec coverage:** все 9 пунктов Этапа 1 из spec'а покрыты задачами 1-12.
2. ✅ **Placeholder scan:** каждый шаг содержит точные команды и код, без TBD/TODO.
3. ✅ **Type consistency:** имена функций/файлов/тегов согласованы (`SCHEMA_MAP`, `START`/`END` маркеры, `PAGES.py` как source of truth).
4. ✅ **Idempotency:** все генераторы можно запускать многократно — повторный запуск не создаёт дубликатов.

Открытые вопросы из spec'а:
- ✅ OG-картинки — задача 4 решает (берём из существующих фото).
- ✅ Шаблон alt-тегов — задача 8 (формула «<Услуга> — работа <Мастер>, Agent Permanent, Москва»).
- ⏳ Я.Вебмастер мета-тег — собираем хеш с пользователя на этапе runbook'а (документировано в Task 11 Step 2).
- ⏳ Я.Бизнес доступ — пользователь подтвердил (вариант A).

---

## Definition of Done

После выполнения всех 12 задач Игорь должен иметь:

✅ Sitemap.xml в корне с 9 URL.
✅ Robots.txt с явным указанием sitemap, закрывающий ИИ-ботов.
✅ Canonical + OpenGraph + Twitter Card на всех 9 production-страницах.
✅ Schema.org JSON-LD (BeautySalon с рейтингом, Course, Organization, BreadcrumbList) на всех страницах.
✅ Title и description оптимизированы под локальные ключи (Москва, Сокол).
✅ 173 пустых alt-тега в `/portfolio/` заполнены осмысленными.
✅ smoke_test.sh — guard против регрессий.
✅ Runbook для Я.Бизнес (что делать руками).
✅ Runbook для Я.Вебмастера.
✅ Деплой на прод, верификация через curl + schema.org-валидатор + OG-валидатор.

После этого можно считать Этап 1 завершённым и переходить к брейнсторму Этапа 2 (семядро + посадочные).
